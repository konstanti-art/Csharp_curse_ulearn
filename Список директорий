public static List<DirectoryInfo> GetAlbums(List<FileInfo> files)
{
    return files
        .Where(file => file.Extension == ".mp3" || file.Extension == ".wav")
        .Select(file => file.Directory)
        .GroupBy(dir => dir.FullName)
        .Select(g => g.First())
        .ToList();
}
