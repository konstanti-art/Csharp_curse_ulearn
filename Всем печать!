public static void Print(params object[] items)
{
    for (var i = 0; i < items.Length; i++)
    {
        if (i > 0)
            Console.Write(", ");
        Console.Write(items[i]);
    }
    Console.WriteLine();
}
